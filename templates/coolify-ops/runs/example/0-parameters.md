# Parameters

- Operation: redeploy the application `acme-website` (the marketing site
  was updated in git; the running container still serves the old build).
- Target: application `acme-website`, project Acme.
- Instance: bound at setup (`instance-url`), Coolify API v1.
- Requested by: the user, 2026-08-02.
