List one directory in the project: subdirectories first (trailing /), then files with sizes.

Paths are confined to the project root. Machine folders (.venv, .git,
__pycache__, node_modules) are hidden.

Args:
    path: Directory relative to the project root (default '.').
